<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="final.camp">
  <select id="getListresveSQL" parameterType="String" resultType="greencamp.resve.model.ResveDTO">
  select * from tb_resve where id=#{id}
  
  </select>
  </mapper>